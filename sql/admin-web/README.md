# Admin Web

This is a Vue 3 + Vite admin scaffold with Pinia, Vue Router, TDesign, and Axios.

## Setup

```bash
npm install
npm run dev
```

## Notes

- Set `VITE_API_BASE_URL` in `.env` if your API host is not `/api`.
- Swagger is available at `/api/docs` once the backend is running.