<t-tab-bar
  value="{{value}}"
  fixed="{{true}}"
  placeholder="{{true}}"
  bordered="{{false}}"
  bind:change="handleChange"
>
  <t-tab-bar-item wx:for="{{tabs}}" wx:key="value" value="{{item.value}}">
    {{item.label}}
  </t-tab-bar-item>
</t-tab-bar>
