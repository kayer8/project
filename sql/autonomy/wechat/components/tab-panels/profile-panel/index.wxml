<app-navbar title="我的" left-arrow="{{false}}" />

<t-pull-down-refresh
  class="panel-refresh"
  enable-passive="{{true}}"
  loadingProps="{{PULL_DOWN_LOADING_PROPS}}"
  value="{{isRefreshing}}"
  bind:refresh="onPanelRefresh"
>
<view class="page-body">
  <view wx:if="{{checking}}" class="profile-section profile-card profile-card--loading">
    <view class="profile-loading__avatar"></view>
    <view class="profile-loading__content">
      <view class="profile-loading__line profile-loading__line--title"></view>
      <view class="profile-loading__line"></view>
      <view class="profile-loading__line profile-loading__line--short"></view>
    </view>
  </view>

  <block wx:else>
    <view class="profile-section profile-header-card">
      <view class="profile-header__top">
        <view class="profile-user">
          <view wx:if="{{sessionUser && sessionUser.avatarUrl}}" class="profile-avatar">
            <image class="profile-avatar__image" src="{{sessionUser.avatarUrl}}" mode="aspectFill" />
          </view>
          <view wx:else class="profile-avatar profile-avatar--placeholder">{{displayInitial}}</view>

          <view class="profile-user__content">
            <view class="profile-user__house">{{houseSummary}}</view>
            <view class="profile-user__meta">
              <view class="profile-chip profile-chip--primary">{{identityLabel}}</view>
              <view class="profile-chip profile-chip--muted">{{mobileText}}</view>
            </view>
          </view>
        </view>
      </view>

      <view
        class="profile-payment-card profile-payment-card--{{paymentTone}}"
        bindtap="handlePaymentTap"
      >
        <view class="profile-payment__left">
          <view class="profile-payment__label">缴费提醒</view>
          <view class="profile-payment__headline">{{paymentHeadline}}</view>
          <view class="profile-payment__desc">{{paymentDescription}}</view>
        </view>

        <view class="profile-payment__right">
          <view class="profile-payment__amount">{{paymentAmountText}}</view>
          <view class="profile-payment__hint">{{houseCountText}}</view>
          <t-icon name="chevron-right" size="28rpx" class="profile-payment__arrow" />
        </view>
      </view>
    </view>

    <view wx:if="{{showAutoBindGuide}}" class="profile-section profile-card2">
      <view class="guide-card">
        <view class="guide-card__badge">推荐</view>
        <view class="guide-card__title">已在物业登记信息，可一键绑定</view>
        <view class="guide-card__desc">
          输入物业登记的手机号，系统会自动校验是否存在对应房屋，并尝试为当前账号完成绑定。
        </view>
        <view class="guide-card__button-wrap">
          <t-button
            class="guide-card__button"
            theme="primary"
            size="large"
            shape="round"
            block
            loading="{{oneClickBinding}}"
            bindtap="handleManualBindTap"
          >
            一键绑定
          </t-button>
        </view>
        <view class="guide-card__footer">
          <text class="guide-card__hint">未匹配到登记信息时，可继续手动绑定房屋。</text>
          <text class="guide-card__link" bindtap="handleAddHouse">手动绑定</text>
        </view>
      </view>
    </view>

    <view class="profile-section profile-card profile-card--menu">
      <view class="menu-list">
        <view
          wx:for="{{personalMenus}}"
          wx:key="key"
          class="menu-item"
          data-url="{{item.url}}"
          data-requires-house="{{item.requiresHouse}}"
          bindtap="handleMenuTap"
        >
          <view class="menu-item__left">
            <view class="menu-item__icon">
              <t-icon name="{{item.icon}}" size="36rpx" class="profile-menu-icon" />
            </view>
            <text class="menu-item__title">{{item.label}}</text>
          </view>
        </view>
      </view>
    </view>

    <view wx:if="{{errorMessage}}" class="profile-section profile-warning">
      {{errorMessage}}
    </view>
  </block>

  <view class="safe-bottom-space"></view>
</view>
</t-pull-down-refresh>
